{
  "name": "movie-app",
  "version": "1.0.0",
  "scripts": {
    "client2": "cd client2 && npm start",
    "server": "cd server && nodemon app.js",
    "dev": "concurrently \"npm run client2\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
